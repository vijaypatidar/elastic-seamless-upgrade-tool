const StorageManager = {
    SESSION_NAME: "session",
    SETUP_SET: "setup_step", // stored in session
    CLUSTER_ID: "cluster_id",
    INFRA_TYPE: "infrastructure_type"
} as const

export default StorageManager
